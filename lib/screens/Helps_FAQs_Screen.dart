import 'package:flutter/material.dart';

class HelpsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Helps & FAQs")),
      body: Center(child: Text("Helps & FAQs Screen Content")),
    );
  }
}
